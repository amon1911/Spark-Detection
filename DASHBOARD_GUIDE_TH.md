# 🎯 คู่มือ Dashboard ระบบ SPARK VISION GUARD

## 📋 ภาพรวมระบบ

Dashboard นี้แสดงข้อมูลการทำงานของเครื่องจักรแบบ Real-time โดยเชื่อมต่อกับระบบ AI ที่ตรวจจับประกายไฟจากกล้อง CCTV

## 🎨 ส่วนประกอบหลักของ Dashboard

### 1. **หัวเรื่อง (Header)**
- ชื่อระบบ: SPARK VISION GUARD
- วันที่และเวลาปัจจุบัน (อัพเดททุกวินาที)
- สัญญาณไฟสถานะ (เขียว = ทำงาน, แดง = หยุด)

### 2. **แผงสถานะเครื่องจักร (ซ้ายมือ)**

#### สถานะเครื่อง
- **RUN** (สีเขียว) = เครื่องกำลังทำงาน
- **STOP** (สีแดง) = เครื่องหยุดทำงาน
- มีไอคอนและแอนิเมชั่นประกอบ

#### สถานะระบบ
- **Backend API**: สถานะการเชื่อมต่อ Backend
- **Camera Feed**: สถานะกล้อง (ACTIVE/ERROR)
- **Database**: สถานะฐานข้อมูล

### 3. **ตัวชี้วัดหลัก (KPI Cards)**

#### 📊 Total Cycles (จำนวนรอบการผลิต)
- แสดงจำนวนรอบที่เครื่องทำงานสำเร็จในวันนี้
- มีแถบความคืบหน้า (Progress Bar)
- อัพเดทแบบ Real-time

#### ⏱️ Total Runtime (เวลาทำงานรวม)
- แสดงเวลาที่เครื่องทำงานจริง (ไม่นับเวลาหยุด)
- รูปแบบ: HH:MM:SS (ชั่วโมง:นาที:วินาที)
- แสดงชั่วโมงเป็นทศนิยมด้วย

#### 📈 Availability (ความพร้อมใช้งาน)
- คำนวณจาก: (เวลาทำงานจริง / เวลางานทั้งหมด) × 100
- เวลางาน: 08:00 - 16:30 น. (8.5 ชั่วโมง)
- ยิ่งสูงยิ่งดี (เป้าหมาย > 85%)

### 4. **กราฟ Timeline (แผนภูมิเส้นเวลา)**

#### การแสดงผล
- **แถบสีเขียว** = ช่วงที่เครื่องทำงาน (RUN)
- **แถบสีเทา** = ช่วงที่เครื่องหยุด (STOP)

#### ข้อมูลที่แสดง
- เวลาเริ่มต้น - สิ้นสุดของแต่ละรอบ
- ระยะเวลาการทำงาน (วินาที)
- หมายเลขรอบการผลิต

#### การใช้งาน
- **Hover** (เลื่อนเมาส์) = ดูรายละเอียด
- **Zoom** = ซูมเข้า-ออก
- **Pan** = เลื่อนดูช่วงเวลาต่างๆ

### 5. **สรุปรายวัน (Daily Summary)**
- จำนวนรอบที่ทำสำเร็จ
- เวลาทำงานรวม (ชั่วโมง)
- อัตราการทำงาน (%)
- สถานะปัจจุบัน

## 🚀 วิธีเปิดใช้งาน

### ขั้นตอนที่ 1: เปิด Backend
```bash
# ที่โฟลเดอร์หลัก (Spark_Detection)
python -m uvicorn app.main:app --reload
```
Backend จะทำงานที่: `http://localhost:8000`

### ขั้นตอนที่ 2: เปิด Frontend
```bash
# เปิด Terminal ใหม่
cd frontend
npm install  # ครั้งแรกเท่านั้น
npm run dev
```
Dashboard จะเปิดที่: `http://localhost:5173`

### ขั้นตอนที่ 3: เปิดเบราว์เซอร์
ไปที่ `http://localhost:5173` จะเห็น Dashboard แบบ Real-time

## 📊 การทำงานของระบบ

### กระบวนการตรวจจับ
```
กล้อง CCTV → AI ตรวจจับประกาย → State Machine → บันทึกฐานข้อมูล → แสดงผล Dashboard
```

### เงื่อนไขการบันทึกรอบ (Cycle)

1. **เริ่มรอบใหม่**: เมื่อตรวจพบประกายไฟ (Spark Detected)
   - สถานะเปลี่ยนจาก STOP → RUN
   - บันทึก Start Time

2. **จบรอบ**: เมื่อไม่พบประกายไฟเกิน 10 วินาที
   - สถานะเปลี่ยนจาก RUN → STOP
   - บันทึก Stop Time
   - คำนวณ Runtime
   - บันทึก 1 Cycle ลงฐานข้อมูล

### เวลาทำงาน
- **เริ่มงาน**: 08:00 น.
- **เลิกงาน**: 16:30 น.
- **รวม**: 8.5 ชั่วโมง/วัน
- นอกเวลางาน: ระบบจะหยุดตรวจจับและปิด Cycle สุดท้ายอัตโนมัติ

## 🎯 การอ่านข้อมูล

### ตัวอย่างการตีความ

#### กรณีที่ 1: เครื่องทำงานปกติ
```
Total Cycles: 45 รอบ
Total Runtime: 06:30:00 (6.5 ชั่วโมง)
Availability: 76.5%
```
**หมายความ**: เครื่องทำงาน 45 รอบ ใช้เวลา 6.5 ชั่วโมง จาก 8.5 ชั่วโมง (ดีมาก)

#### กรณีที่ 2: เครื่องมีปัญหา
```
Total Cycles: 12 รอบ
Total Runtime: 02:15:00 (2.25 ชั่วโมง)
Availability: 26.5%
```
**หมายความ**: เครื่องทำงานได้เพียง 12 รอบ ใช้เวลาเพียง 2.25 ชั่วโมง (ต้องตรวจสอบ)

## 🔧 การแก้ปัญหา

### ปัญหา: Dashboard ไม่แสดงข้อมูล

**วิธีแก้**:
1. ตรวจสอบ Backend ทำงานหรือไม่ → เปิด `http://localhost:8000`
2. ดู Console ของเบราว์เซอร์ (F12)
3. ตรวจสอบ Network Tab ว่า API ตอบกลับหรือไม่

### ปัญหา: กราฟไม่แสดง

**สาเหตุที่เป็นไปได้**:
1. ยังไม่มีข้อมูล Cycle (รอเครื่องทำงานรอบแรก)
2. อยู่นอกเวลางาน (ก่อน 08:00 หรือหลัง 16:30)
3. กล้องไม่ทำงาน

### ปัญหา: สถานะกล้องเป็น ERROR

**วิธีแก้**:
1. ตรวจสอบ RTSP URL ในไฟล์ `.env`
2. ทดสอบเปิดกล้องด้วยโปรแกรมอื่น (VLC)
3. ตรวจสอบ Network connection

## 📈 การปรับแต่ง

### เปลี่ยนสีธีม
แก้ไขใน [`App.jsx`](src/App.jsx):
```javascript
// สีเมื่อเครื่องทำงาน
from-green-900/20 to-green-950/10  // พื้นหลัง
text-green-400                      // ข้อความ
bg-green-500                        // แถบ

// สีเมื่อเครื่องหยุด
from-red-900/20 to-red-950/10      // พื้นหลัง
text-red-400                        // ข้อความ
bg-red-500                          // แถบ
```

### เปลี่ยนความถี่ในการอัพเดท
แก้ไขใน [`App.jsx`](src/App.jsx:42):
```javascript
const interval = setInterval(fetchData, 2000); // 2000 = 2 วินาที
```

### ปรับเป้าหมาย Cycles
แก้ไขใน [`App.jsx`](src/App.jsx:11):
```javascript
const WORK_HOURS = 8.5; // เปลี่ยนตามเวลางานจริง
```

## 🎓 คำศัพท์เทคนิค

| คำศัพท์ | ความหมาย |
|---------|----------|
| **Cycle** | รอบการทำงาน (เริ่ม → จบ) |
| **Runtime** | เวลาที่เครื่องทำงานจริง |
| **Downtime** | เวลาที่เครื่องหยุด |
| **Availability** | ความพร้อมใช้งาน (%) |
| **State** | สถานะ (RUN/STOP) |
| **Spark Detection** | การตรวจจับประกายไฟ |
| **Real-time** | แบบเรียลไทม์ (ทันที) |

## 📞 ติดต่อ

หากมีปัญหาหรือข้อสงสัย:
1. ตรวจสอบ Log ของ Backend
2. ดู Console ของ Browser (F12)
3. ตรวจสอบการเชื่อมต่อ Database

---

**สร้างโดย**: Spark Vision Guard Team  
**อัพเดทล่าสุด**: ธันวาคม 2025
