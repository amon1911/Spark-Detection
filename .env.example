# Database Configuration (สำหรับ local PostgreSQL container)
POSTGRES_USER=admin
POSTGRES_PASSWORD=your_strong_password_change_me!
POSTGRES_DB=machine_monitor_db

# PgAdmin (optional - Web UI สำหรับดู/แก้ DB)
PGADMIN_DEFAULT_EMAIL=admin@example.com
PGADMIN_DEFAULT_PASSWORD=your_pgadmin_password_here

# Spark Detection App
RTSP_URL=rtsp://your-camera-ip:554/stream  # URL ของกล้องจริง หรือ '0' เพื่อปิด vision module ชั่วคราว

# Production Database (สำหรับ cloud เช่น Railway/Render)
# DATABASE_URL=postgresql://username:password@host:port/database_name
# (set ใน cloud dashboard แทน ไม่ต้องใส่ .env)